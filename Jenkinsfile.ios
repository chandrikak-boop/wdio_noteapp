pipeline {
    agent any

    environment {
        NODEJS_HOME = tool name: 'node20', type: 'NodeJS'
        PATH = "${env.NODEJS_HOME}/bin:${env.PATH}"
        ANDROID_HOME = '/Users/testvagranttechnologies/Library/Android/sdk' // optional if not needed
    }

    stages {
        stage('Checkout SCM') {
            steps {
                checkout scm
            }
        }

        stage('Install Dependencies') {
            steps {
                sh 'npm ci'
            }
        }

        stage('Verify Xcode') {
            steps {
                sh 'xcode-select -p'
                sh 'xcodebuild -version'
            }
        }

        stage('Run WDIO iOS Tests') {
            steps {
                sh 'npx wdio config/wdio.ios.conf.ts'
            }
        }
    }

    post {
        always {
            archiveArtifacts artifacts: '**/reports/*.xml', allowEmptyArchive: true
            junit '**/reports/*.xml'
        }
        failure {
            echo 'iOS tests failed!'
        }
        success {
            echo 'iOS tests passed successfully!'
        }
    }
}
